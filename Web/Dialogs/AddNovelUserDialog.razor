@using Web.Services
@inject NovelUserService NovelUserService
@inject IDialogService DialogService
<MudDialog>
    <DialogContent>
        <MudTextField @ref="_usernameTextField" Label="Username" Variant="Variant.Text"></MudTextField>
    </DialogContent>
</MudDialog>

@code {
    [CascadingParameter] MudDialogInstance MudDialog { get; set; } = null!;
    
    [Parameter] public int NovelId { get; set; }

    MudTextField<string>? _usernameTextField;
    
    protected override async Task OnInitializedAsync()
    {
        
    }

    void Back() => MudDialog.Cancel();

}